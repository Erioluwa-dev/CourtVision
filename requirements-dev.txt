# Test-only dependencies for the logic test suite (pytest).
# The full runtime stack (ultralytics, opencv, etc.) lives in
# requirements.txt; the unit tests do not need the ML models.
pytest==9.1.1
numpy==2.5.1
opencv-python-headless==5.0.0.93
scikit-learn==1.9.0
